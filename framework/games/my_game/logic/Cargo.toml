[package]
name = "my_game_logic"
version = "0.1.0"
edition = "2024"

[dependencies]
game = { path = "../../../game" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
